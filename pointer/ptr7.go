package main

func ptr7(){
	// 구조체 변수를 별도로 생성하지 않고 곧바로 포인터 변수에 구조체를 생성해 주소를 초기값으로 대입하는 방법

	// < 구조체 변수 생성 후 포인터 변수 >
/*
	var b test
	var p *test = &b

	// < 구조체와 포인터 변수를 동시에 선언하기 > 

	var c *test = &test{} // 이렇게 하면 포인터 변수만으로 구조체의 필드값에 접근하고 변경할 수 있다
*/
}